Flask==3.1.2
flask-cors==6.0.1
pymongo==4.7.0
python-dotenv==1.1.1
gunicorn==22.0.0

# Optional/resolve: these are normally pulled in by Flask; keep if you prefer pinned transitive deps
# Jinja2==3.1.6
# Werkzeug==3.1.3


